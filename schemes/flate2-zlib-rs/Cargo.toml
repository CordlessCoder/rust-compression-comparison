[package]
name = "flate2-zlib-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
common = { path = "../../common" }
flate2 = { version = "1.1", default-features = false, features = ["zlib-rs"] }
